import styled from "styled-components";
export const CustomSpan = styled.span`
  display: block;
  font-size: 1.3em;
  margin: 5px;
  font-weight: 600;
  width: 100%;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;
